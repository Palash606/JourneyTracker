package com.major.resume.repository.student;

import org.springframework.data.jpa.repository.JpaRepository;

import com.major.resume.entities.student.StudentSkills;

public interface StudentSkillsRepository extends JpaRepository<StudentSkills, Integer>{

}
