package com.major.resume.repository.student;

import org.springframework.data.jpa.repository.JpaRepository;

import com.major.resume.entities.student.StudentWeakness;

public interface StudentWeaknessRepository extends JpaRepository<StudentWeakness, Integer> {

}
